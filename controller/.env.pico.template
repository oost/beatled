# WiFi networks now live in the shared controller/.env.wifi (copy
# .env.wifi.template). They're sourced automatically before this file.
# Anything you set here still overrides the shared values if you need a
# per-device exception.
BEATLED_SERVER_NAME=
NUM_PIXELS=30
WS2812_PIN=0
